function santasCookies(array){

    let baches = array.shift();

    for(let i = 0; i < baches*3; i += 3){
        let flour = array[i];
        let sugar = array[i+1];
        let cocoa = array[i+2];
    }
}
santasCookies([2,
    200,
    300,
    500,
    100,
    200,
    50
    ])